apiVersion: v1
data:
  environment.json: |
    {
      "KEYCLOAK_URL": "https://mytest.com",
      "KEYCLOAK_CLIENT_ID": "my-test",
      "KEYCLOAK_REALM": "pulse8_wealth_cockpit"
    }
kind: ConfigMap
metadata:
  name: {{project_id}}-config
  namespace: default
